/* Prints the travel advice to the DOM. */
function printTravelAdvice(trainTravelAdvice) {
	// Fetch the first possible train advice.
	trainAdvice = trainTravelAdvice['ReisMogelijkheid'][0];
	
	console.log("Geplande reistijd" + trainAdvice['GeplandeReisTijd']);
	
	// Fetch all trains that are part of the journey (e.g. 1 train to Amsterdam Centraal, 1 train from AC to Science Park).
	trains = trainAdvice['ReisDeel'];
	
	// Loop over all trains.
	for (var i = 0, l = trains.length; i < l; i++) {
		var trainType = trains[i]['VervoerType'];
		var startStation = trains[i]['ReisStop'][0];
		var endStation = $(trains[i]['ReisStop']).get(-1);
		
				
	}
}